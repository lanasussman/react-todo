// eslint-disable-next-line react/prop-types
function TodoListItem({ todo }) {
  return (
    // eslint-disable-next-line react/prop-types
    <li>{todo.title}</li>
  );
}

export default TodoListItem;
